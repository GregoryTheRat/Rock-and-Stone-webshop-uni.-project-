<%= form_with(model: carry) do |form| %>
  <% if carry.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(carry.errors.count, "error") %> prohibited this carry from being saved:</h2>

      <ul>
        <% carry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :supplier_id, style: "display: block" %>
    <%= form.number_field :supplier_id %>
  </div>

  <div>
    <%= form.label :mit, style: "display: block" %>
    <%= form.text_field :mit %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
