<%= form_with(model: szallito) do |form| %>
  <% if szallito.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(szallito.errors.count, "error") %> prohibited this szallito from being saved:</h2>

      <ul>
        <% szallito.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :km_ar, style: "display: block" %>
    <%= form.text_field :km_ar %>
  </div>

  <div>
    <%= form.label :ar_kg, style: "display: block" %>
    <%= form.text_field :ar_kg %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
