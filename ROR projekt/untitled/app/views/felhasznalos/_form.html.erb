<%= form_with(model: felhasznalo) do |form| %>
  <% if felhasznalo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(felhasznalo.errors.count, "error") %> prohibited this felhasznalo from being saved:</h2>

      <ul>
        <% felhasznalo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nev, style: "display: block" %>
    <%= form.text_field :nev %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.label :jelszo, style: "display: block" %>
    <%= form.text_field :jelszo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
